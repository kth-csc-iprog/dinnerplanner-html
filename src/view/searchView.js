class SearchView {
  constructor(container, model) {
    this.container = container;
    this.model = model;
  }

  render(dishId) {
  }
  
  afterRender() {
  }

  update(payload) {
    // TODO lab3
  }
}
