class OverviewView {
    constructor(container, model) {
        this.container = container;
        this.model = model;
    }

    render(isPrint) {
        this.afterRender();
    }

    afterRender() {
    }
}
